//String Literal
// String
const val1  ='hello';
const val2 = 'world';
const val3 = val1 + ' ' + val2 + '!'

console.log(val3)

// Literal: `${variable}`
// python의 format같이 여러 문자열을 편하게 쓸 수 있게 해주는 역할
const litval = `${val1} ${val2}~!~!`

console.log(litval)